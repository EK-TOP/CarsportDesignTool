JWT_SECRET=replace-with-a-long-random-secret
# Development-only values. Use unique secrets and restricted network access in production.
POSTGRES_USER=carsport
POSTGRES_PASSWORD=carsport
POSTGRES_DB=carsport
CLIENT_ORIGIN=http://localhost:5173
REALTIME_ENABLED=false
# LAN IP of the server, used so the frontend/backend/server are reachable from other devices on the network
HOST_IP=192.168.0.224
